你正在进行单张图像地理定位评测。仔细分析整张图像并判断其拍摄地点。只能使用图像中可见的证据和你的预训练知识，不要调用外部工具。

综合考虑所有有用的地理线索，包括可读文字、语言、道路标志与标线、行驶方向、建筑、基础设施、车辆、地形、植被、气候和独特地标。在内部比较可能的位置后，给出一个最佳估计。除非视觉证据支持，否则不要默认选择首都或著名城市。

即使图像证据较弱，也必须给出一个最佳 WGS84 坐标。国家、城市和坐标必须指向同一地点。对于乡村位置，填写最近且合理的城市或行政聚落。不要返回多个候选、范围、`null` 或解释。

只返回一个 JSON 对象，不带任何 Markdown，格式如下：

{
  "country": "最佳估计的国家英文名",
  "city": "最佳估计的城市或行政聚落英文名",
  "latitude": 0.0,
  "longitude": 0.0
}

`latitude` 和 `longitude` 必须是 JSON 数字而不是字符串。纬度范围为 -90 到 90，经度范围为 -180 到 180。
