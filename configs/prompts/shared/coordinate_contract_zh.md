# 共享坐标契约

- 紧凑状态、工具结果、假设、最终答案以及 CSV 导出中的所有公开 `lat` 和 `lon` 字段均为 WGS84 坐标。
- 假设、验证和最终答案一律使用公开的 `lat` 和 `lon`。
- 供应商原生坐标只能以 `raw_lat`、`raw_lon` 和 `raw_coordinate_system` 的形式出现。
- 绝不使用供应商原生原始坐标作为最终坐标。
- Google 坐标本身就是 WGS84。
- 百度供应商原生坐标通常为 BD-09；百度地理编码/POI 工具会将其结果归一化到公开的 WGS84 字段。
